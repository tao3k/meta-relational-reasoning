[package]
name = "gql"
description = "ISO GQL facade with optional reasoning backends"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true

[features]
default = ["ascent"]
ascent = ["dep:gql-ascent"]

[dependencies]
gql-ascent = { workspace = true, optional = true }
gql-core.workspace = true
gql-reasoning.workspace = true

[dev-dependencies]
criterion.workspace = true

[lints]
workspace = true
