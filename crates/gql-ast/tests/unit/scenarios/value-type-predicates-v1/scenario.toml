id = "value-type-predicates-v1"
title = "ISO GQL value-type predicate hot path"
policy_ids = ["RUST-AGENT-GQL-AST-PERF-001"]
agent_goal = "Parse and lower both typed markers and the complete value-type predicate family with zero fallback."
reference_repositories = ["opengql/grammar", "rust-analyzer/rowan"]
reference_patterns = [
  "Gerbil AOT owns typed predicate shape, primary-operand boundary, and typed recovery",
  "The Rowan parser reuses PropertyValueType nodes instead of introducing a second type grammar",
  "AST and canonical IR retain negation, operand, structural value type, source order, and source span",
]
inputs = "inputs"
