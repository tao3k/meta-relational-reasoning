id = "iso-aggregate-functions-v1"
title = "ISO GQL aggregate function family hot path"
policy_ids = ["RUST-AGENT-GQL-AST-PERF-001"]
agent_goal = "Parse and lower COUNT(*), ALL/DISTINCT general-set aggregates, and binary percentile aggregates with zero fallback."
reference_repositories = ["opengql/grammar", "rust-analyzer/rowan"]
reference_patterns = [
  "Gerbil AOT owns aggregate functions, quantifier policy, arity, CST shapes, and typed recovery",
  "The Rowan parser emits typed aggregate and set-quantifier nodes without generic-call fallback",
  "AST and canonical IR retain function, quantifier, argument order, count-star identity, and source span",
]
inputs = "inputs"
